# 9.22 OOP class
c++中的string类型可以直接进行赋值，“+”等操作串联，原因在于操作符的重载

precision的作用范围是比较长的，而setw作用仅下一个输出成立，我们可以设定一个函数来抽象该处理

**string**
- +，+=
- substr
- find

**STL**
- Container
- Algorithm
- Iterator

vector中的capacity是成倍增长的