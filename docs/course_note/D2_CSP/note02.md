# Chapter 2:Instruction

## Register
- 现代RISC架构通常采用32个通用寄存器的设计（32位或64位）
- 16位也被称为half word, 64位为double word
- 要处理数据，需要将数据存入寄存器，然后对寄存器进行操作，然后再存回内存中，而不是直接在内存中操作
- 内存地址表示：基址+偏移量
- R型指令操作码是0110011，0111011（word版本）操作码不够表示指令内容，还需要功能码
- I型指令里面有12位用来表示立即数

- 内存以字节为单位
- 小端：一串指令的低位放置在内存的低地址